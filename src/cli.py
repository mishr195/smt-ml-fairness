"""
Main Execution Gateway.

Parses command-line arguments using argparse to accept the target model path, domain configuration
presets, and mathematical fairness epsilon overrides, routing them directly into the verifier core.
"""
